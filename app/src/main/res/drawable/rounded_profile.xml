<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Color transparente para el fondo -->

    <!-- Define los bordes redondeados -->
    <corners android:radius="30dp" />
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Ajusta el color y ancho del borde según tus preferencias -->
</shape>
